[target]
name = "sim"
schema_version = 2
base = "common"

[values]
"ledger.target.board" = "sim"

# Assignment ledger는 target-plan 전용 support table을 해석하지 않지만, 같은
# target document를 공유하므로 이 closed root table을 거부해서는 안 된다.
[support]
provider_owner = "sys.arch.fixture"
consumer_owner = "sys.board.fixture"
role = "architecture.facade.v1"
facade_include_root = "sys/include/parus/arch/fixture"
required_kapi = "parus.arch.fixture.support.v1"
