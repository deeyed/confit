schema_version = 2

[option."choice.first"]
type = "bool"
default = false
write_domain = "profile"
owner = "confit"
since = "0.2.0"
stability = "stable"

[choice."choice.mode"]
member_type = "bool"
members = ["choice.first"]
cardinality = "one-or-more"
